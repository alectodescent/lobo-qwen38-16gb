# Model directory

Run `..\tools\download-balanced-model.ps1` to obtain the single preassembled Balanced/MTP model here. Model files are ignored by Git. See [Model files](../docs/MODEL_FILES.md) for exact names, sizes, hashes, sources, and the deterministic assembly fallback.
